import React from 'react';
import TokenSwap from '../components/TokenSwap'; // Adjust the import path if necessary
import '../styles/styles.css'; // Ensure the path is correct

const Trade = () => {
  return (
    <main className="trade-page">
      <TokenSwap />
    </main>
  );
};

export default Trade;
